# Linux / SSH (читаем с хоста)
filenames:
  - /var/log/auth.log
  - /var/log/syslog
labels:
  type: syslog
---
# Caddy (читаем через Docker API)
source: docker
container_name:
 - caddy
labels:
  type: caddy
