---
- name: Deploy Syncthing
  ansible.builtin.include_role:
    name: utils
    tasks_from: docker_service
  vars:
    utils_docker_service_name: "{{ syncthing_service_name }}"
    utils_docker_service_dir: "{{ syncthing_compose_dir }}"
    utils_docker_service_data_dirs:
      - "{{ syncthing_data_dir }}"
    utils_docker_service_compose_template: "{{ playbook_dir }}/roles/syncthing/templates/docker-compose.yml.j2"
